# Assign items to containers

Assign each of $[N]$ labeled items to one of $[B]$ containers through a map $\mathcal{X}$, with constraints that for any
distinct $i,j$ where $i+j\le[N]$ the values $\mathcal{X}(i),\mathcal{X}(j),\mathcal{X}(i+j)$ are not all identical.

[//]: # (Generated using nemotron-3-nano:latest from D001 description.en.md and model.mzn)
