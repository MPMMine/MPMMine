# Packing items into containers

The problem requires assigning $n$ distinct items, each labeled with a unique integer from 1 to $n$, into $k$ containers
such that for any three labels $x$, $y$, $z$ where $x + y = z$, they are not all assigned to the same container.

[//]: # (Generated using deepseek-r1:latest from D001 description.en.md and model.mzn)
